# React

React 単体で見たい内容は、UI 部品の書き方や props の扱いです。  
ただしこのリポジトリでは `Next.js` とセットで読む方が実務的なので、詳細は統合ページにまとめています。

- 統合ページ: [React / Next.js](./react-nextjs.md)

## React だけ先に思い出すなら

- `component`: 画面部品
- `props`: 親から子へ渡す値
- JSX: JavaScript の中に HTML 風構文を書くもの
- `{...}`: JSX の中で JavaScript の値を使う場所
